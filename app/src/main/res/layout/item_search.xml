<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/ivSearch"
        android:layout_width="match_parent"
        android:layout_height="160dp"
        android:scaleType="centerCrop"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvTitleSearch"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="it's for your Ow good"
        android:ellipsize="end"
        android:maxLines="3"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:textSize="20sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ivSearch" />

    <TextView
        android:id="@+id/tvGenres"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="Drama|Fantasy|Romace"
        android:textColor="#2E2424"
        android:ellipsize="end"
        android:maxLines="3"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tvTitleSearch" />

    <RatingBar
        android:id="@+id/rbSearch"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:numStars="5"
        style="?android:attr/ratingBarStyleIndicator"
        app:layout_constraintLeft_toRightOf="@+id/tvTitleSearch"
        app:layout_constraintTop_toBottomOf="@id/ivSearch" />
</androidx.constraintlayout.widget.ConstraintLayout>